# Simple laravel currency application

1. clone repo ```git clone https://github.com/kvnick/laravel-currency laravel-currency```
2. run
    ```
    composer install
    npm install
    cp .env.example .env
    php artisan key:generate
    php artisan serve
    ```
3. done
